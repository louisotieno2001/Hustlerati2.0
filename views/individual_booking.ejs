<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Individual Booking - Hustlerati 2.0</title>
    <link rel="icon" href="/images/logo.png" type="image/x-icon">
    <link rel="apple-touch-icon" href="/images/logo.png">
    <link rel="stylesheet" href="/stylesheets/default.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
</head>

<body>
    <%- include('partials/header') %>

    <main class="individual-booking-main">
        <div class="container">
            <div class="booking-form-container">
                <div class="form-header">
                    <h1><i class="fas fa-user"></i> Individual Booking</h1>
                    <p>Book this property for yourself. Your contribution must be at least the full rent amount.</p>
                </div>

                <div class="property-summary">
                    <h2><%= property.title %></h2>
                    <p><strong>Location:</strong> <%= property.location %></p>
                    <p><strong>Price:</strong> <%= property.price %> <%= property.currency %> per month</p>
                    <p><strong>Description:</strong> <%= property.description %></p>
                </div>

                <form id="individual-booking-form" action="/book" method="POST">
                    <input type="hidden" name="type" value="individual">
                    <input type="hidden" name="propertyId" value="<%= property.id %>">
                    <input type="hidden" name="propertyName" value="<%= property.title %>">
                    <input type="hidden" name="propertyLocation" value="<%= property.city %>">
                    <input type="hidden" name="propertyPrice" value="<%= property.price %>">
                    <input type="hidden" name="propertyDescription" value="<%= property.description %>">

                    <div class="form-group">
                        <label for="contribution">Your Contribution (Your currency)</label>
                        <input type="number" id="contribution" name="price" min="<%= property.price %>" step="0.01" required>
                        <small class="form-hint">Minimum contribution: <%= property.price %> <%= property.currency %></small>
                        <div id="contribution-error" class="error-message" style="display: none;">Contribution must be at least <%= property.price %> <%= property.currency %>.</div>
                    </div>

                    <button type="submit" class="btn btn-primary btn-large">
                        <i class="fas fa-calendar-check"></i> Confirm Booking
                    </button>
                </form>
            </div>
        </div>
    </main>

    <%- include('partials/footer') %>
    <script src="/javascript/individual_booking.js"></script>
    <script src="/javascript/index.js"></script>
    <script src="/javascript/accessibility.js"></script>
    <script src="/javascript/localization.js"></script>
</body>

</html>
