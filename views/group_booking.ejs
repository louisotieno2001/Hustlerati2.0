<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Group Booking - Hustlerati 2.0</title>
    <link rel="icon" href="/images/logo.png" type="image/x-icon">
    <link rel="apple-touch-icon" href="/images/logo.png">
    <link rel="stylesheet" href="/stylesheets/default.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
</head>

<body>
    <%- include('partials/header') %>

    <main class="group-booking-main">
        <div class="container">
            <div class="booking-form-container">
                <div class="form-header">
                    <h1><i class="fas fa-users"></i> Group Booking</h1>
                    <p>Join others in sharing this property. Your contribution must be at least USD $20.</p>
                </div>

                <div class="property-summary">
                    <h2><%= property.title %></h2>
                    <p><strong>Location:</strong> <%= property.location %></p>
                    <p><strong>Price:</strong> <%= property.price %> <%= property.currency %> per month</p>
                    <p><strong>Description:</strong> <%= property.description %></p>
                </div>

                <form id="group-booking-form" action="/book" method="POST">
                    <input type="hidden" name="type" value="group">
                    <input type="hidden" name="propertyId" value="<%= property.id %>">
                    <input type="hidden" name="propertyName" value="<%= property.title %>">
                    <input type="hidden" name="propertyLocation" value="<%= property.city %>">
                    <input type="hidden" name="propertyPrice" value="<%= property.price %>">
                    <input type="hidden" name="propertyDescription" value="<%= property.description %>">

                    <div class="form-group">
                        <label for="contribution">Your Contribution (Your currency)</label>
                        <input type="number" id="contribution" name="price" min="20" step="0.01" required>
                        <small class="form-hint">Minimum contribution: USD$20</small>
                        <div id="contribution-error" class="error-message" style="display: none;">Contribution must be at least USD$20.</div>
                    </div>

                    <button type="submit" class="btn btn-primary btn-large">
                        <i class="fas fa-user-plus"></i> Join Group Booking
                    </button>
                </form>
            </div>
        </div>
    </main>

    <%- include('partials/footer') %>
    <script src="/javascript/group_booking.js"></script>
    <script src="/javascript/index.js"></script>
    <script src="/javascript/accessibility.js"></script>
    <script src="/javascript/localization.js"></script>
</body>

</html>
